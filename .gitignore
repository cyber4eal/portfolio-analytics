__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/
build/
dist/
.DS_Store

# Built payload carries live holdings and values - never commit it.
site/data.json
.cache/
# The CSV snapshot is the same live book as data.json, in another format.
# Force-add it deliberately if you ever want a dated copy versioned.
data/*.csv
# Ledger and pension hold live personal figures, same as data.json.
data/transactions.jsonl
data/pension.json
data/*.jsonl
logs/
